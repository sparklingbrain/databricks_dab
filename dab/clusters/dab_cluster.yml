variables:
  dab_cluster_task_1:
    description: This is the path to the dab cluster.
    type: complex
    default:
      spark_version: 15.4.x-scala2.12
      node_type_id: i3.xlarge
      data_security_mode: SINGLE_USER
      autoscale:
        min_workers: 1
        max_workers: 4

  dab_cluster_task_2:
    description: This is the path to the dab cluster.
    type: complex
    default:
      spark_version: 15.4.x-scala2.12
      node_type_id: i3.xlarge
      data_security_mode: SINGLE_USER
      autoscale:
        min_workers: 1
        max_workers: 1

  # existing_dab_cluster:
  #   description: This is existing dab cluster.
  #   lookup:
  #     cluster: dab-cluster-1


